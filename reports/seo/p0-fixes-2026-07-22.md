# P0 SEO/GEO fixes — 22 luglio 2026

## Cosa è stato implementato nel repo

| Fix | File / azione | Effetto su GitHub Pages (live oggi) | Effetto su Workers Assets |
|---|---|---|---|
| `agenzie-web-rho` noindex + canonical + refresh | `agenzie-web-rho.html`, `config/pseo-governance.js` | Sì (dopo deploy HTML) | + 301 da `_redirects` |
| Sitemap esclude `noindex` in `<head>` | `generate-sitemap.js` | Sì (dopo regen + deploy `sitemap.xml`) | Idem |
| `llms.txt` solo hub indexable + count reale | `scripts/generate-llms-index.js`, `llms.txt` | Sì (dopo deploy) | Idem |
| `llms-full.txt` rigenerato | `scripts/generate-llms-full.js` | Sì | Idem |
| `.assetsignore` amplia (server, config, data, scripts…) | `.assetsignore` | **No** (solo wrangler) | Sì: path sensibili non uploadati |
| Documentazione redirect di zona CF | `docs/deploy/CLOUDFLARE-ZONE-REDIRECTS.md` | Richiede azione manuale in dashboard | Bridge fino a migrazione |
| Note in `_redirects` | `_redirects` | No finché origine = GH Pages | Sì dopo migrazione |
| Test regressione | `tests/seo-regressions.test.js`, `tests/pseo-governance-regressions.test.js` | CI | CI |
| Audit 80 GEO indexable | `scripts/audit-indexable-geo.js` → `reports/seo/geo-indexable-audit.md` | Report locale | — |

## Comandi

```bash
npm run build:sitemap          # o: node generate-sitemap.js
npm run build:llms
npm run build:llms-full
npm run audit:geo
node tests/seo-regressions.test.js
node tests/pseo-governance-regressions.test.js
node tests/seo-smoke.test.js
```

## Azioni ancora a carico tuo (non automatizzabili solo da codice)

1. **Deploy** del branch con questi file su GitHub Pages / prod.
2. **Cloudflare Single Redirects** (finché non migri a Workers): seguire `docs/deploy/CLOUDFLARE-ZONE-REDIRECTS.md`  
   - `/agenzie-web-rho.html` → `/agenzia-web-rho.html`  
   - `/dist/*` → strip prefix  
   - `/consulenza-digitale-*` → `/consulenze-*`
3. **Migrazione Workers Assets** (`docs/deploy/MIGRAZIONE-CLOUDFLARE-PAGES.md`) per attivare `_headers` + `_redirects` + `.assetsignore` nativi.
4. Upgrade **contenuto** sulle GEO (vedi audit): title lunghi, Speakable T1, cluster seo-locale/ecommerce troppo simili.

## Verifica post-deploy

```bash
curl -sI "https://www.webnovis.com/agenzie-web-rho.html" | head -8
# ideale: 301 Location: .../agenzia-web-rho.html
# minimo: 200 con noindex + canonical a agenzia-web-rho

curl -s "https://www.webnovis.com/agenzie-web-rho.html" | rg -o 'name="robots"[^>]+|rel="canonical"[^>]+'

curl -s "https://www.webnovis.com/llms.txt" | rg -n "Nerviano|48\\+|sitemap"
# non deve citare Nerviano noindex né "48+"

curl -sI "https://www.webnovis.com/server.js" | head -3
# dopo migrazione Workers: 404; oggi ancora 200 su GH Pages
```
