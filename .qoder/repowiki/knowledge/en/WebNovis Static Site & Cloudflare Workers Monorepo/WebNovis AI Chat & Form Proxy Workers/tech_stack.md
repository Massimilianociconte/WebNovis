Cloudflare Workers runtime with Wrangler v3 (`compatibility_date: 2026-07-28`, `nodejs_compat` flag). AI backend is Google Gemini Generative Language API (`generativelanguage.googleapis.com/v1beta/models/...`). Persistent state uses Cloudflare KV (`SESSIONS` namespace) for sessions, rate-limit counters, and search-result cache. External integrations: Brevo SMTP for lead emails and Web3Forms for form delivery.