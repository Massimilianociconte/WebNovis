---
kind: external_dependency
name: IndexNow — instant indexing protocol for Bing, Yandex, Naver
slug: indexnow
category: external_dependency
category_hints:
    - vendor_identity
scope:
    - '**'
---

Domain ownership verified via a `.txt` file placed in the site root (already committed to the repo). The key is stored in `INDEXNOW_KEY` in `.env.example`. Used to notify search engines of new/updated pages for faster indexing beyond standard crawl cycles.