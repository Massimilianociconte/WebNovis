name: Quality Gate

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run quality gate
        run: npm run ci:quality:dist

      - name: Verify build did not mutate tracked sources
        run: git diff --exit-code

      - name: Upload sanitized public artifact
        uses: actions/upload-artifact@v4
        with:
          name: webnovis-public-dist
          path: dist/
          include-hidden-files: true
          if-no-files-found: error

      # Gira su ogni push a main: è il gate che rileva header di sicurezza
      # mancanti in produzione (CSP/Permissions-Policy) e sorgenti esposti.
      # Su PR da fork non gira (la produzione non è ancora cambiata).
      - name: Verify production headers
        if: github.event_name != 'pull_request'
        run: npm run verify:prod-headers
